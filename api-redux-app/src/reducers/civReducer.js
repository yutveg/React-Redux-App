const initialState = {
  civData: { name: "", army_type: "", team_bonus: "", civilization_bonus: [] },
  idQuery: 1
};

export const civReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
